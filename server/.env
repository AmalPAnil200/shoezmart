# ── For Render (Production) ──
# Paste your Render Internal/External Database URL here:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# ── For Local Development ──
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=shoezstack
DB_PORT=5432
DB_DIALECT=postgres
# Set to "true" if your local Postgres requires SSL
# DB_SSL=true

JWT_SECRET=your_jwt_secret_here